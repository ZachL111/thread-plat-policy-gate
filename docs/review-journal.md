# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its platform engineering focus without claiming live deployment or external usage.

## Cases

- `baseline`: `rollout width`, score 163, lane `ship`
- `stress`: `quota pressure`, score 188, lane `ship`
- `edge`: `route drift`, score 213, lane `ship`
- `recovery`: `secret scope`, score 242, lane `ship`
- `stale`: `rollout width`, score 214, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
